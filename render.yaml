services:
  - type: web
    name: palubot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: SAMU_NUMBER
        value: "166"
      - key: WEBHOOK_URL
        sync: false
